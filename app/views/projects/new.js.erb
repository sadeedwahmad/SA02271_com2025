$('#new-project a').hide().parent().append("<%= j render 'projects/form', project: @project %>")
$('#form_modal').on('shown.bs.modal', function(){
    $('form[data-validate]').enableClientSideValidations();
}).modal('show');
