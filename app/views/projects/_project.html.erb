<tr>
  <td><%= project.name %></td>
  <td><%= project.code %></td>
  <td><%= project.description %></td>

  <td><%= link_to 'View and manage project and tasks', project %></td>
  <td><%= link_to 'Edit', edit_project_path(project) %></td>
  <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?', remote: true }, class: "delete"%></td>
</tr>